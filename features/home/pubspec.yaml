name: home_screen
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  core:
    path: ../../core
  core_ui:
    path: ../../core_ui
  domain:
    path: ../../domain
  data:
    path: ../../data
  navigation:
    path: ../../navigation

dev_dependencies:
  flutter_lints: ^3.0.1
  mobx_codegen: ^2.6.1
  auto_route_generator: ^7.3.2
  build_runner: ^2.4.8
